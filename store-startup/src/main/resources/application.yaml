spring:
  application:
    name: store-service

  datasource:
    url: jdbc:postgresql://localhost:5432/storeservice
    username: myuser
    password: mypassword
    driver-class-name: org.postgresql.Driver